(ns flambo-cassandra-bindings.core-test
  (:require [clojure.test :refer :all]
            [flambo-cassandra-bindings.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
